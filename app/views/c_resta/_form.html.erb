<%= form_for @c_restum, html: { multipart: true } do |f| %>
  <% if @c_restum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c_restum.errors.count, "error") %> prohibited this c_restum from being saved:</h2>

      <ul>
      <% @c_restum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img %><br>
    <%= f.file_field :img %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
