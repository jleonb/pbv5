<%= form_for @slider_serv, html: { multipart: true } do |f| %>
  <% if @slider_serv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider_serv.errors.count, "error") %> prohibited this slider_serv from being saved:</h2>

      <ul>
      <% @slider_serv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img %><br>
    <%= f.file_field :img %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
