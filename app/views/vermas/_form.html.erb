<%= form_for(@verma) do |f| %>
  <% if @verma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@verma.errors.count, "error") %> prohibited this verma from being saved:</h2>

      <ul>
      <% @verma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
