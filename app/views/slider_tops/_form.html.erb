<%= form_for @slider_top, html: { multipart: true } do |f| %>
  <% if @slider_top.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider_top.errors.count, "error") %> prohibited this slider_top from being saved:</h2>

      <ul>
      <% @slider_top.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :n_contrato %><br>
    <%= f.text_field :n_contrato %>
  </div>
  <div class="field">
    <%= f.label :img %><br>
    <%= f.file_field :img %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
